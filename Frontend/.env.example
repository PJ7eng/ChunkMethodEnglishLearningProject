VITE_API_BASE_URL=http://localhost:3000
VITE_APP_PLATFORM=web
VITE_SENTRY_DSN=

# Android production/debug bundles use .env.android-production
# (copy from .env.android-production.example). That file is gitignored.
# Native builds cannot use localhost; they must point at the Railway HTTPS API.
