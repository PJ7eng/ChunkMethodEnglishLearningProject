# Copy to .env.android-production (gitignored). Do not commit the real URL.
# Use the same Railway HTTPS origin as Cloudflare Pages VITE_API_BASE_URL.
# No trailing slash. Must not be localhost.
VITE_API_BASE_URL=
